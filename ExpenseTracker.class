// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
import java.awt.BorderLayout;
import java.awt.Component;
import java.awt.Font;
import java.awt.GridLayout;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.util.ArrayList;
import java.util.List;
import javax.swing.BorderFactory;
import javax.swing.DefaultListModel;
import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JList;
import javax.swing.JOptionPane;
import javax.swing.JPanel;
import javax.swing.JScrollPane;
import javax.swing.JTextField;
import javax.swing.SwingUtilities;

public class ExpenseTracker extends JFrame implements ActionListener {
   private JTextField nameField;
   private JTextField amountField;
   private JButton addButton;
   private JButton deleteButton;
   private DefaultListModel<String> expenseListModel;
   private JList<String> expenseList;
   private JLabel totalLabel;
   private double total = 0.0;
   private List<Double> amounts = new ArrayList();

   public ExpenseTracker() {
      this.setTitle("Expense Tracker");
      this.setDefaultCloseOperation(3);
      this.setSize(400, 450);
      this.setLayout(new BorderLayout());
      this.setLocationRelativeTo((Component)null);
      JPanel var1 = new JPanel(new GridLayout(3, 2, 10, 10));
      var1.setBorder(BorderFactory.createEmptyBorder(10, 10, 10, 10));
      var1.add(new JLabel("Expense Name:"));
      this.nameField = new JTextField();
      var1.add(this.nameField);
      var1.add(new JLabel("Amount (₹):"));
      this.amountField = new JTextField();
      var1.add(this.amountField);
      this.addButton = new JButton("Add Expense");
      this.addButton.addActionListener(this);
      var1.add(this.addButton);
      this.deleteButton = new JButton("Delete Selected");
      this.deleteButton.addActionListener(this);
      var1.add(this.deleteButton);
      this.add(var1, "North");
      this.expenseListModel = new DefaultListModel();
      this.expenseList = new JList(this.expenseListModel);
      this.add(new JScrollPane(this.expenseList), "Center");
      this.totalLabel = new JLabel("Total: ₹0.00", 0);
      this.totalLabel.setFont(new Font("Arial", 1, 16));
      this.totalLabel.setBorder(BorderFactory.createEmptyBorder(10, 10, 10, 10));
      this.add(this.totalLabel, "South");
   }

   public void actionPerformed(ActionEvent var1) {
      if (var1.getSource() == this.addButton) {
         String var2 = this.nameField.getText().trim();
         String var3 = this.amountField.getText().trim();
         if (!var2.isEmpty() && !var3.isEmpty()) {
            try {
               double var4 = Double.parseDouble(var3);
               this.expenseListModel.addElement(var2 + " - ₹" + var4);
               this.amounts.add(var4);
               this.total += var4;
               this.totalLabel.setText(String.format("Total: ₹%.2f", this.total));
               this.nameField.setText("");
               this.amountField.setText("");
            } catch (NumberFormatException var6) {
               JOptionPane.showMessageDialog(this, "Enter a valid number for amount!", "Error", 0);
            }
         } else {
            JOptionPane.showMessageDialog(this, "Please enter both name and amount.", "Warning", 2);
         }
      } else if (var1.getSource() == this.deleteButton) {
         int var7 = this.expenseList.getSelectedIndex();
         if (var7 != -1) {
            this.total -= (Double)this.amounts.get(var7);
            this.amounts.remove(var7);
            this.expenseListModel.remove(var7);
            this.totalLabel.setText(String.format("Total: ₹%.2f", this.total));
         } else {
            JOptionPane.showMessageDialog(this, "Select an expense to delete.", "Info", 1);
         }
      }

   }

   public static void main(String[] var0) {
      SwingUtilities.invokeLater(() -> {
         (new ExpenseTracker()).setVisible(true);
      });
   }
}
